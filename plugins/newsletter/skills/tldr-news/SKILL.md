---
name: tldr-news
description: "TLDR AI 뉴스레터 수집. TLDR에서 AI 관련 새 뉴스를 확인하고 싶을 때 사용."
---

# TLDR AI 수집 스킬

## 실행 절차

### 1단계: 스크립트 실행

```bash
python3 {SKILL_DIR}/scripts/tldr_collector.py 2>/dev/null
```

- 출력이 있으면 새 뉴스가 있는 것. 출력이 비어있으면 새 뉴스 없음.
- 중복 제거와 스폰서 글 제외는 스크립트가 자동 처리.

### 2단계: 결과 전달

- 출력이 **비어있으면** → 어떤 메시지도 보내지 마라. 완전히 침묵.
- 출력이 있으면 → `TLDR AI 새 뉴스` 헤더와 함께 전달.

전송 형식:
- 스크립트 출력의 각 줄은 `번호. [제목](URL) - [시간]` 형식이다.
- 제목만 한국어로 번역하고, **URL은 그대로 유지**하라.
- 최종 형식: `[번역된 제목](원본URL)`
- **모든 항목에는 반드시 URL이 포함되어야 한다.** URL이 없는 항목을 전송하는 것은 절대 금지.
- 스크립트 출력 데이터만 사용. 데이터를 지어내지 마라.

## 금지 사항

- 브라우저를 사용하지 마라.
- 스크립트 출력에 없는 데이터를 만들어내지 마라.
- 사용자에게 질문하지 마라.
- **URL을 생략하거나 제거하지 마라.** 각 항목의 `(URL)` 부분을 반드시 포함하라.
