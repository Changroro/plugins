---
description: Show current plugin configuration
---

Read and display the current configuration from `${CLAUDE_PLUGIN_ROOT}/config.json`.

Show the user:
1. Current configuration values
2. What each setting controls
3. How to change settings using `/claude_plugin_bch:config-set`

Configuration file location: `${CLAUDE_PLUGIN_ROOT}/config.json`

$ARGUMENTS
